description: >
  Sample orbs.

parameters:
  sample_key_value:
    type: string
    default: sample_value
    description: sample parameter
steps:
  - run:
      name: Sample
      command: |
        echo "<<parameters.sample_key_value>>"
  - run:
      name: Include File
      environment:
        SAMPLE_KEY_VALUE: echo "<<parameters.sample_key_value>>"
      command: <<include(scripts/sample.sh)>>
  - run:
      name: Include File2
      environment:
        SAMPLE_KEY_VALUE: "SAMPLE_SAMPLE"
      command: <<include(scripts/sample.sh)>>
